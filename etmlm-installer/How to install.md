### Implementing the .ico



###### Just run it as an administrator. With this installer, .etmlm files will have their own icon in VSC, and Visual Studio Code won't assign them the default icon it uses for unrecognized files and .txt files.

###### 

###### If your desktop background or taskbar disappears during installation, do not worry! It will restart automatically. If it doesn't, remember to refresh your Explorer from the Task Manager (Ctrl + Shift + Esc -> Run new task -> type explorer.exe)

